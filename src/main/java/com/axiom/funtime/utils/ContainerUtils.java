package com.axiom.funtime.utils;

import net.minecraft.client.MinecraftClient;
import net.minecraft.util.math.BlockPos;

public class ContainerUtils {
    public static BlockPos findNearestContainer(MinecraftClient client, int radius) {
        // scan for chests, shulker boxes, etc.
        return null;
    }

    public static void openContainer(BlockPos pos) {
        // send interaction packet
    }
}
